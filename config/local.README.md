**Before pushing this repo make sure any files that need encryption are encrypted**
While in the directory run a `git-crypt status -e`.  If your file is not listed move it to /tmp,add the original file name in `.gitattributes` then move it back.  It should show up now.  If not try copying it to a new name then back.  

